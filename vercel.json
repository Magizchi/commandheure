{
    "version":2,
    "github":{
        "enable": false
    },
    "build":  [{
            "src":"app/api/src/main.ts",
            "use":"@vercel/node"
        }],
    "routes":[
        {"src": "./(.*)", "dest":"app/api/src/main.ts", "method":["GET","POST","PUT"]}
    ]
}